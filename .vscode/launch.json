{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug C++ with IO",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}\\${fileBasenameNoExtension}.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "externalConsole": false,
            "preLaunchTask": "Build C++ file"
        }
    ]
}
